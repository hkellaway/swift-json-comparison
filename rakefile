desc "Builds and runs tests."
# Usage: rake build
task :build do
  system "xctool test -project SwiftJSONComparison/SwiftJSONComparison.xcodeproj -scheme SwiftJSONComparison -sdk iphonesimulator8.3"
  puts "Build attempt complete."
end
